<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
    <cron>
        <url>/api/admin/watchdog/check</url>
        <description>Check Keepalive Watchdogs. Cron schedule must match the configured keepalive ping interval.</description>
        <schedule>every 30 minutes</schedule>
    </cron>
</cronentries>